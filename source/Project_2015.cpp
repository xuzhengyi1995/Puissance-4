// Project_2015.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"
#include<iostream>
#include<string>
#include"game.h"


using namespace std;

int main()
{
	game new_game;
	/*node p1;
	int i, j;
	for (i = 0; i < 6; i++)
		for (j = 0; j < 7; j++)
			p1.map[i][j] = 0;
	p1.map[5][1] = 5;
	p1.map[5][3] = 5;
	p1.map[5][4] = 5;
	p1.choose = 1;
	p1.side = yellow;
	AI a1(p1,3);
	a1.search();*/
	/*new_game.first();
	new_game.clean_text();
	char aa[7] = { 0 };
	new_game.place_it(red, 0, 0);
	new_game.place_it(red, 0, 1);
	new_game.place_it(yellow, 6, 0);
	*/
}

